services:
  convert:
    build:
      context: ..
      args:
        VITE_COMMIT_SHA: docker
      dockerfile: Dockerfile    # relativt compose-filens katalog (docker/), inte kontexten
    image: convert:dev
